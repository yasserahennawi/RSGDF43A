import IoC from 'AppIoC';
import ForbiddenError from '../../errors/ForbiddenError';

export default class Create{{ModelName}}Command {
  constructor({{modelName}}Repository) {
    this.{{modelName}}Repository = {{modelName}}Repository;
  }

  async execute(viewer, {{modelName}}) {
    const deleted = await this.{{modelName}}Repository.remove(viewer, {{modelName}}Id);
    return deleted;
  }
}

IoC.singleton('remove{{ModelName}}Command', ['{{modelName}}Repository'], Create{{ModelName}}Command);
