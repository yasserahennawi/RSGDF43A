import Validator from './Validator';
import IoC from 'AppIoC';

export default class {{ModelName}}Validator extends Validator {
  constructor() {
    super();
  }

  validate({{modelName}}) {
    let promises = [
    ];

    return this.getErrors(promises);
  }
}

IoC.singleton('{{modelName}}Validator', [], {{ModelName}}Validator);
