import ForbiddenError from '../../errors/ForbiddenError';

export default class Update{{ModelName}}Command {
  constructor({{modelName}}Repository) {
    this.{{modelName}}Repository = {{modelName}}Repository;
  }

  /**
   * @param {Object} viewer Viewer trying to update {{modelName}}
   * @param {String} id
   * @param {Object} attrs
   * @return {Promise} promise resolve with the updated {{modelName}} object
   */
  async execute(viewer, id, {
    name,
  }) {
    const {{modelName}} = await this.{{modelName}}Repository.update(viewer, id, {
      name,
    });

    return {{modelName}};
  }
}
