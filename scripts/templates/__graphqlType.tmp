import {
  GraphQLBoolean,
  GraphQLFloat,
  GraphQLID,
  GraphQLInt,
  GraphQLList,
  GraphQLNonNull,
  GraphQLObjectType,
  GraphQLSchema,
  GraphQLString
} from 'graphql';

import {
  connectionArgs,
  connectionDefinitions,
  connectionFromArray,
  fromGlobalId,
  globalIdField,
  mutationWithClientMutationId,
  nodeDefinitions,
  cursorForObjectInConnection
} from 'graphql-relay';

import IoC from 'AppIoC';

export const {{modelName}}Type = (
  {{modelName}}Model,
  nodeInterface,
  imageType
) => new GraphQLObjectType({
  name: '{{ModelName}}',
  description: 'A person who uses our app',
  fields: () => ({
    id: globalIdField('{{ModelName}}'),
    name: { type: GraphQLString },
  }),
  interfaces: [nodeInterface],
  isTypeOf: obj => obj instanceof {{modelName}}Model,
});

IoC.callable('{{modelName}}Type', ['{{modelName}}Model', 'nodeInterface', 'imageType'], {{modelName}}Type);
